# HANDOFF.md - PoTS Decision Engine

## Session summary (2026-04-29) - Implementation complete

### What was done this session

- **Capital allocation respects decisions** (`engine.py:116`, notebook cell 13)
  - Added `decisions: list | None = None` to `capital_allocation()`; Kill assets skipped in funding loop
  - Plot shows killed assets in red with `//` hatching and "KILLED" label
  - Budget $350M now allocates $300M (Asset C excluded as Kill)

- **Net EVSI in Value of Information** (`engine.py:233`, notebook cell 21)
  - Added `cost_per_patient: float = 0.0` to `value_of_information()`; returns `net_evsi = max(evsi - cost_per_patient * N, 0)`
  - `cost_per_patient = 0.05` set in notebook ($50K/patient, rare disease context)
  - 3-panel VoI plot: decision flip probability, gross EVSI, net EVSI
  - Summary table adds "Net EVSI" and "Optimal N"; narrative recommends based on net EVSI

- **Section 12 Portfolio Summary Dashboard** (new cells 24-25)
  - Summary table: Asset, Phase, Indication, PoTS, Decision, eNPV, Funded, VoI Recommendation
  - Key metrics: total funded eNPV ($512M), budget utilization (86%), P(positive), median portfolio value, 90% range
  - Top data gap callout: Asset E (PNH) net EVSI $10.4M

- **MIT License** - created `LICENSE` (copyright Christopher S. Nelson, 2026)

- **README** - updated VoI table with gross/net EVSI, Optimal N; updated narrative; license section; removed ipywidgets

- Notebook executed clean, all 12 plots regenerated. Committed and pushed as `6e3eba8`.

---

## What's left / next steps (priority order)

1. **Portfolio summary visual** - Section 12 is text-only. A heatmap-style table or traffic-light grid would be stronger for fellowship reviewers.
2. **Sensitivity of cost_per_patient** - $50K/patient is a placeholder. A table showing net EVSI at $25K/$50K/$100K would make the assumption explicit.
3. **Streamlit deployment** - app.py works locally; Streamlit Community Cloud blocked. Try Hugging Face Spaces next (see CLAUDE.md).
4. **Lower priority engine improvements** (unchanged from previous session):
   - Repurpose viability threshold hardcoded at 0.15 - should be phase-dependent
   - `smart_decision` docstring says "CI-based" but logic is mean-based
   - No pytest suite for engine.py
   - Multi-stage eNPV model (phase-by-phase cost discounting)

---

## Key decisions made

- **Kill exclusion at allocation time, not decision time** - decision engine still returns Kill; capital allocation loop skips them. Preserves separation of concerns.
- **`net_evsi = max(evsi - cost_per_patient * N, 0)`** - floored at zero; negative net EVSI means "don't enroll more."
- **`cost_per_patient = 0.05`** - placeholder; documented as assumption in notebook.

---

## Decision logic (unchanged)

| Condition | Decision |
|-----------|----------|
| mean > go_t | Go |
| mean < kill_t | Kill |
| between thresholds AND similarity_score >= 0.65 | Repurpose |
| between thresholds, similarity < 0.65 | Continue |

Phase thresholds: Phase I (kill=0.10, go=0.15), Phase II (kill=0.10, go=0.25), Phase III (kill=0.15, go=0.40)

---

## File structure

```
portfolio_demo.ipynb    - Main notebook (sections 1-12)
engine.py               - Computation engine (shared by notebook and app.py)
app.py                  - Streamlit app (runs locally, deployment blocked)
data/assets.csv         - 5 assets
data/repurpose_map.csv  - 10 repurpose candidates
plots/                  - 12 PNGs regenerated by notebook
README.md               - Overview with embedded plots
LICENSE                 - MIT License
```

---

## Blockers

- Streamlit Community Cloud deployment still blocked (see CLAUDE.md Future Directions)
