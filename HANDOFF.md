# HANDOFF.md - PoTS Decision Engine

## Session summary (2026-04-29)

### What was done
- Full codebase review (engine.py, notebook, data files, README, plots)
- Identified 12 improvements, prioritized by fellowship-reviewer impact
- Created implementation plan for the top 3 changes plus MIT license

### Plan ready for implementation (`.claude/plans/sparkling-dancing-galaxy.md`)

1. **Capital allocation respects decisions** - `capital_allocation()` currently funds anything with positive eNPV, even killed assets. Add `decisions` parameter so only Go/Continue/Repurpose assets compete for budget. Files: `engine.py:116`, notebook cell 13.

2. **Trial cost in VoI** - EVSI doesn't subtract the cost of enrolling patients. Add `cost_per_patient` parameter ($50K default for rare disease), compute net EVSI, update summary to show optimal N. Files: `engine.py:233`, notebook cell 21.

3. **Summary dashboard cell** - No quick-glance output. Add Section 12 with combined decision/eNPV/VoI table, portfolio metrics, and top risk callout.

4. **MIT License** - New `LICENSE` file.

---

## Lower priority improvements (not planned)

- Repurpose viability threshold hardcoded at 0.15 - should be phase-dependent
- `smart_decision` docstring says "CI-based" but logic is mean-based
- Monte Carlo seed reuse across stress test scenarios
- Failure propagation disconnected from downstream analysis
- No pytest suite for engine.py
- Multi-stage eNPV model (phase-by-phase cost discounting)

---

## Decision logic (current, unchanged)

| Condition | Decision |
|-----------|----------|
| mean > go_t | Go |
| mean < kill_t | Kill |
| between thresholds AND similarity_score >= 0.65 | Repurpose |
| between thresholds, similarity < 0.65 | Continue |

Phase thresholds: Phase I (kill=0.10, go=0.15), Phase II (kill=0.10, go=0.25), Phase III (kill=0.15, go=0.40)

---

## File structure

```
portfolio_demo.ipynb    - Main notebook (sections 1-11)
engine.py               - Computation engine (shared by notebook and app.py)
app.py                  - Streamlit app (runs locally, deployment blocked)
data/assets.csv         - 5 assets
data/repurpose_map.csv  - 10 repurpose candidates
plots/                  - 12 PNGs regenerated by notebook
README.md               - Overview with embedded plots
```

---

## Blockers

- None for the planned work
- Streamlit Community Cloud deployment still blocked (see CLAUDE.md)
